<?xml version="1.0" encoding="UTF-8"?>
<persistence version="2.0" xmlns="http://java.sun.com/xml/ns/persistence" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="         http://java.sun.com/xml/ns/persistence         http://java.sun.com/xml/ns/persistence/persistence_2_0.xsd">
  <persistence-unit name="primary" transaction-type="JTA">
    <!-- If you are running in a production environment, add a managed data 
			source, the example data source is just for proofs of concept! -->
    <provider>org.hibernate.ejb.HibernatePersistence</provider>
	<!--
	<jta-data-source>java:jboss/datasources/uqasarFileBasedDS</jta-data-source>
	-->
    <jta-data-source>java:jboss/datasources/uqasarDS${ds.name}</jta-data-source>
    <properties>
      <!-- Properties for Hibernate -->
      <property name="hibernate.dialect" value="${dialect}" />
      <property name="hibernate.hbm2ddl.auto" value="${ddl}" />
	  <property name="hibernate.show_sql" value="${show.sql}" />
	  <property name="hibernate.format_sql" value="${format.sql}" />
      <!-- Hibernate-Search configuration -->
      <property name="hibernate.search.default.indexBase" value="${lucene.index.base}"/>
      <property name="hibernate.search.default.directory_provider" value="${lucene.dir.provider}"/>
    </properties>
  </persistence-unit>
</persistence>
